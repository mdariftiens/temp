app/Repository/Eloquent/BankProgramRepo.php : 3304

if(env('CLIENT') == 'yesbank' && $cust_id_enable == 1){
                $bc = BandConfiguration::select('id')->where('uuid', $request->program_map_id)->first();
                $rules['cust_id'] = 'required|unique:band_configurations,cust_id'.(isset($bc->id)?',' . $bc->id: '');
            }
